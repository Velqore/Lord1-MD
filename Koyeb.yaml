name: Lord1-MD
type: app
regions:
  - fra # Frankfurt (choose your preferred region)
ports:
  - 8080:8080
build:
  type: node
  files:
    - package.json
    - package-lock.json
    - main.js
    - .env
plan: web # Ensuring it's a web service, not a worker
